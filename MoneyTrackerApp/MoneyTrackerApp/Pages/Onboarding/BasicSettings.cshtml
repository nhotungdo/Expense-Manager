@page
@model MoneyTrackerApp.Pages.Onboarding.BasicSettingsModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Basic Settings - Money Tracker</title>
    <link rel="stylesheet" href="~/css/onboarding.css" />
</head>
<body class="onboarding-page">
    <div class="onboarding-container">
        <!-- Progress indicator -->
        <div class="progress-bar">
            <div class="progress-fill" style="width: 33.33%"></div>
        </div>

        <div class="onboarding-content">
            <div class="step-header">
                <span class="step-number">Step 1 of 4</span>
                <h1 class="step-title">Basic Settings</h1>
                <p class="step-description">Let's personalize your experience</p>
            </div>

            <form id="basicSettingsForm" class="onboarding-form">
                <!-- Currency Selection -->
                <div class="form-group">
                    <label for="currency" class="form-label">
                        <span class="label-icon">💱</span>
                        Main Currency
                    </label>
                    <select id="currency" name="currency" class="form-select" required>
                        <option value="VND" selected>Vietnamese Dong (₫)</option>
                        <option value="USD">US Dollar ($)</option>
                        <option value="EUR">Euro (€)</option>
                        <option value="GBP">British Pound (£)</option>
                        <option value="JPY">Japanese Yen (¥)</option>
                    </select>
                    <small class="form-hint">This will be your default currency for all transactions</small>
                </div>

                <!-- Language Selection -->
                <div class="form-group">
                    <label for="language" class="form-label">
                        <span class="label-icon">🌐</span>
                        Language
                    </label>
                    <select id="language" name="language" class="form-select" required>
                        <option value="vi" selected>Tiếng Việt</option>
                        <option value="en">English</option>
                    </select>
                    <small class="form-hint">Choose your preferred language</small>
                </div>

                <!-- Theme Selection -->
                <div class="form-group">
                    <label class="form-label">
                        <span class="label-icon">🎨</span>
                        Theme
                    </label>
                    <div class="theme-selector">
                        <div class="theme-option" data-theme="light">
                            <div class="theme-preview light">
                                <div class="preview-bar"></div>
                                <div class="preview-content"></div>
                            </div>
                            <span class="theme-name">Light</span>
                            <input type="radio" name="theme" value="light" checked>
                        </div>
                        <div class="theme-option" data-theme="dark">
                            <div class="theme-preview dark">
                                <div class="preview-bar"></div>
                                <div class="preview-content"></div>
                            </div>
                            <span class="theme-name">Dark</span>
                            <input type="radio" name="theme" value="dark">
                        </div>
                    </div>
                </div>

                <!-- Error message -->
                <div id="errorMessage" class="error-message hidden"></div>

                <!-- Navigation buttons -->
                <div class="form-actions">
                    <button type="button" class="btn-secondary" onclick="window.location.href='/Onboarding/Welcome'">
                        ← Back
                    </button>
                    <button type="submit" class="btn-primary">
                        Next →
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script src="~/js/onboarding-basic-settings.js"></script>
</body>
</html>
