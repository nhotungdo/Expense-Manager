@page
@model MoneyTracker.Pages.IndexModel
@{
    ViewData["Title"] = "MoneyTracker - Quản lý Chi tiêu Thông minh";
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/index.css" />
</head>

<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
                <span class="logo-icon">💰</span>
                MoneyTracker
            </div>
            <div class="nav-buttons">
                <a href="#" class="btn btn-outline" onclick="showLogin()">Đăng nhập</a>
                <a href="#" class="btn btn-primary" onclick="showRegister()">Đăng ký</a>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <div class="hero-text">
                <h1 class="hero-title">Quản lý tài chính thông minh</h1>
                <p class="hero-subtitle">
                    Theo dõi chi tiêu, lập kế hoạch ngân sách và đạt được mục tiêu tài chính của bạn một cách dễ dàng
                    với MoneyTracker.
                </p>
                <ul class="hero-features">
                    <li>Theo dõi thu chi tự động</li>
                    <li>Phân tích chi tiêu thông minh</li>
                    <li>Báo cáo chi tiết trực quan</li>
                    <li>Đặt mục tiêu tiết kiệm</li>
                </ul>
                <div class="hero-cta">
                    <a href="#" class="btn btn-cta btn-primary" onclick="showRegister()">Bắt đầu miễn phí</a>
                    <a href="#features" class="btn btn-cta btn-outline">Tìm hiểu thêm</a>
                </div>
            </div>
            <div class="hero-visual">
                <div class="phone-mockup">
                    <div class="mockup-screen">
                        <div class="mockup-header">
                            <h3>💰 Dashboard</h3>
                        </div>
                        <div class="mockup-stats">
                            <div class="mockup-stat">
                                <div class="stat-icon" style="color: #4CAF50;">💰</div>
                                <div class="stat-value" style="color: #4CAF50;">25.5M</div>
                                <div class="stat-label">Thu nhập</div>
                            </div>
                            <div class="mockup-stat">
                                <div class="stat-icon" style="color: #f44336;">💸</div>
                                <div class="stat-value" style="color: #f44336;">18.7M</div>
                                <div class="stat-label">Chi tiêu</div>
                            </div>
                            <div class="mockup-stat">
                                <div class="stat-icon" style="color: #2196F3;">💎</div>
                                <div class="stat-value" style="color: #2196F3;">6.8M</div>
                                <div class="stat-label">Còn lại</div>
                            </div>
                            <div class="mockup-stat">
                                <div class="stat-icon" style="color: #FF9800;">🏦</div>
                                <div class="stat-value" style="color: #FF9800;">45.2M</div>
                                <div class="stat-label">Tiết kiệm</div>
                            </div>
                        </div>
                        <div
                            style="background: #f0f0f0; height: 60px; border-radius: 8px; display: flex; align-items: center; justify-content: center; color: #666; font-style: italic;">
                            📊 Biểu đồ chi tiêu
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features" id="features">
        <div class="features-container">
            <h2 class="features-title">Tại sao chọn MoneyTracker?</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <span class="feature-icon">📊</span>
                    <h3 class="feature-title">Phân tích thông minh</h3>
                    <p class="feature-description">
                        AI phân tích thói quen chi tiêu và đưa ra lời khuyên cá nhân hóa để bạn tiết kiệm hiệu quả hơn.
                    </p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">🔐</span>
                    <h3 class="feature-title">Bảo mật tuyệt đối</h3>
                    <p class="feature-description">
                        Mã hóa end-to-end, xác thực 2 lớp và tuân thủ các tiêu chuẩn bảo mật cao nhất.
                    </p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">📱</span>
                    <h3 class="feature-title">Đa nền tảng</h3>
                    <p class="feature-description">
                        Sử dụng mọi lúc mọi nơi với ứng dụng web, mobile và desktop. Đồng bộ real-time.
                    </p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">🎯</span>
                    <h3 class="feature-title">Mục tiêu thông minh</h3>
                    <p class="feature-description">
                        Đặt mục tiêu tiết kiệm, theo dõi tiến độ và nhận thông báo nhắc nhở thông minh.
                    </p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">🏦</span>
                    <h3 class="feature-title">Kết nối ngân hàng</h3>
                    <p class="feature-description">
                        Tự động đồng bộ với 50+ ngân hàng và ví điện tử lớn tại Việt Nam.
                    </p>
                </div>
                <div class="feature-card">
                    <span class="feature-icon">📈</span>
                    <h3 class="feature-title">Báo cáo chi tiết</h3>
                    <p class="feature-description">
                        Báo cáo trực quan với biểu đồ đẹp mắt, xuất PDF/Excel và chia sẻ dễ dàng.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Login Modal -->
    <div class="modal" id="loginModal">
        <div class="modal-content">
            <button class="close-btn" onclick="closeModal()">&times;</button>
            <h2 class="modal-title">Đăng nhập</h2>
            <form id="loginForm">
                <div class="form-group">
                    <label class="form-label">Email hoặc số điện thoại</label>
                    <input type="text" class="form-input" placeholder="example@email.com" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Mật khẩu</label>
                    <input type="password" class="form-input" placeholder="••••••••" required>
                </div>
                <button type="submit" class="btn-submit">Đăng nhập</button>
                <div class="form-divider">
                    <span>hoặc</span>
                </div>
                <div class="form-switch">
                    Chưa có tài khoản? <a href="#" onclick="showRegister()">Đăng ký ngay</a>
                </div>
            </form>
        </div>
    </div>

    <!-- Register Modal -->
    <div class="modal" id="registerModal">
        <div class="modal-content">
            <button class="close-btn" onclick="closeModal()">&times;</button>
            <h2 class="modal-title">Đăng ký</h2>
            <form id="registerForm">
                <div class="form-group">
                    <label class="form-label">Họ tên</label>
                    <input type="text" class="form-input" placeholder="Nguyễn Văn A" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Email</label>
                    <input type="email" class="form-input" placeholder="example@email.com" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Số điện thoại</label>
                    <input type="tel" class="form-input" placeholder="0123456789" required>
                </div>
                <div class="form-group">
                    <label class="form-label">Mật khẩu</label>
                    <input type="password" class="form-input" placeholder="••••••••" required>
                </div>
                <button type="submit" class="btn-submit">Tạo tài khoản</button>
                <div class="form-divider">
                    <span>hoặc</span>
                </div>
                <div class="form-switch">
                    Đã có tài khoản? <a href="#" onclick="showLogin()">Đăng nhập</a>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Modal functions
        function showLogin() {
            document.getElementById('registerModal').classList.remove('active');
            document.getElementById('loginModal').classList.add('active');
        }

        function showRegister() {
            document.getElementById('loginModal').classList.remove('active');
            document.getElementById('registerModal').classList.add('active');
        }

        function closeModal() {
            document.getElementById('loginModal').classList.remove('active');
            document.getElementById('registerModal').classList.remove('active');
        }

        // Close modal when clicking outside
        window.addEventListener('click', function (e) {
            const loginModal = document.getElementById('loginModal');
            const registerModal = document.getElementById('registerModal');

            if (e.target === loginModal) {
                closeModal();
            }
            if (e.target === registerModal) {
                closeModal();
            }
        });

        // Form submissions
        document.getElementById('loginForm').addEventListener('submit', function (e) {
            e.preventDefault();
            alert('🎉 Đăng nhập thành công!\n\nBạn sẽ được chuyển đến dashboard để bắt đầu quản lý chi tiêu.');
            closeModal();
            // Redirect to login page
            window.location.href = '/Login';
        });

        document.getElementById('registerForm').addEventListener('submit', function (e) {
            e.preventDefault();
            alert('🎉 Đăng ký thành công!\n\nChào mừng bạn đến với MoneyTracker!\nHãy bắt đầu hành trình quản lý tài chính thông minh.');
            closeModal();
            // Redirect to login page
            window.location.href = '/Login';
        });

        // Navbar scroll effect
        window.addEventListener('scroll', function () {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.style.background = 'rgba(255, 255, 255, 0.95)';
                navbar.style.color = '#333';
                navbar.querySelector('.logo').style.color = '#667eea';
            } else {
                navbar.style.background = 'rgba(255, 255, 255, 0.1)';
                navbar.querySelector('.logo').style.color = 'white';
            }
        });

        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
</body>

</html>